{{ 'page-customer.css' | asset_url | stylesheet_tag }}

<div class="auth-page">
  <div class="auth-split">

    <div class="auth-split__form">
      <div class="auth-form-inner">

        <a href="{{ routes.root_url }}" class="auth-form__logo">
          {%- if section.settings.logo != blank -%}
            <img
              src="{{ section.settings.logo | img_url: '240x80' }}"
              alt="{{ shop.name | escape }}"
              width="120" height="40"
              loading="eager">
          {%- else -%}
            <span style="font-family:var(--font-heading);font-size:var(--text-xl);color:var(--color-primary);">{{ shop.name }}</span>
          {%- endif -%}
        </a>

        <p class="auth-form__eyebrow">{{ section.settings.eyebrow }}</p>
        <h1 class="auth-form__title">{{ section.settings.heading }}</h1>
        <p class="auth-form__subtitle">{{ section.settings.subheading }}</p>

        {%- form 'reset_customer_password' -%}

          {%- if form.errors -%}
            <div class="auth-form__errors" role="alert">
              <ul>
                {%- for error in form.errors -%}
                  <li>{{ form.errors.messages[error] }}</li>
                {%- endfor -%}
              </ul>
            </div>
          {%- endif -%}

          <div class="form-field form-field--password">
            <label for="reset-password">New Password</label>
            <input
              type="password"
              id="reset-password"
              name="customer[password]"
              autocomplete="new-password"
              required
              minlength="5"
              placeholder="Minimum 5 characters"
              {%- if form.errors contains 'password' -%} aria-invalid="true" {%- endif -%}>
            <button type="button" class="form-field__toggle js-toggle-password" data-target="reset-password">Show</button>
          </div>

          <div class="form-field form-field--password">
            <label for="reset-password-confirm">Confirm New Password</label>
            <input
              type="password"
              id="reset-password-confirm"
              name="customer[password_confirmation]"
              autocomplete="new-password"
              required
              placeholder="Repeat password"
              {%- if form.errors contains 'password_confirmation' -%} aria-invalid="true" {%- endif -%}>
            <button type="button" class="form-field__toggle js-toggle-password" data-target="reset-password-confirm">Show</button>
          </div>

          <div class="auth-form__submit">
            <button type="submit" class="btn btn-primary btn-lg" style="width:100%;">
              Set New Password
            </button>
          </div>

        {%- endform -%}

      </div>
    </div>

    <div class="auth-split__visual">
      {%- if section.settings.background_image != blank -%}
        <img
          src="{{ section.settings.background_image | img_url: '900x1200', crop: 'center' }}"
          alt="" width="900" height="1200"
          loading="lazy" aria-hidden="true">
      {%- endif -%}
      <div class="auth-split__visual-overlay"></div>
      <div class="auth-split__visual-content">
        {%- if section.settings.quote != blank -%}
          <p class="auth-split__visual-quote">{{ section.settings.quote }}</p>
          <cite class="auth-split__visual-cite">{{ section.settings.quote_author }}</cite>
        {%- endif -%}
      </div>
    </div>

  </div>
</div>

<script>
document.querySelectorAll('.js-toggle-password').forEach(function (btn) {
  btn.addEventListener('click', function () {
    var inp = document.getElementById(btn.dataset.target);
    var isHidden = inp.type === 'password';
    inp.type = isHidden ? 'text' : 'password';
    btn.textContent = isHidden ? 'Hide' : 'Show';
  });
});
</script>

{% schema %}
{
  "name": "Reset Password",
  "tag": null,
  "settings": [
    { "type": "image_picker", "id": "logo", "label": "Logo" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "✦ RESET PASSWORD" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Create a New Password" },
    { "type": "text", "id": "subheading", "label": "Subheading", "default": "Enter your new password below to regain access to your account." },
    { "type": "image_picker", "id": "background_image", "label": "Background image" },
    { "type": "text", "id": "quote", "label": "Quote", "default": "आरोग्यं परमं भाग्यं — Health is the greatest wealth." },
    { "type": "text", "id": "quote_author", "label": "Attribution", "default": "— Charaka Samhita" }
  ],
  "blocks": [],
  "presets": [{ "name": "Reset Password" }]
}
{% endschema %}
